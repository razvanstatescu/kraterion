/**
 * Unused in v5 — the close moved to S08_Billing. Kept as a no-op for
 * layout-key stability if anything still references the import.
 */
import React from "react";
import { AbsoluteFill } from "remotion";

export const S09_Close: React.FC = () => <AbsoluteFill />;
