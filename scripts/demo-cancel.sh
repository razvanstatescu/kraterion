#!/usr/bin/env bash
set -euo pipefail
# TODO: demonstrate that SharedBlobs persist after subscription cancellation
# (twist 1 — see /docs/implementation-plan.md §2.6).
echo "demo-cancel.sh — not yet implemented"
